# Server Configuration
API_HOST=0.0.0.0
API_PORT=8000

# CORS Settings (comma-separated origins, or * for all)
CORS_ORIGINS=*

# Available Transforms (comma-separated)
AVAILABLE_TRANSFORMS=trim,lower,upper

# Available Comparison Rules (JSON array)
AVAILABLE_RULES=["equals","case_insensitive_equals","contains"]

# Feature Flags
ENABLE_LOGGING=true
MAX_CSV_SIZE_MB=50
